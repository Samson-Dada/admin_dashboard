import React from "react";

const Navbar = () => {
	return (
		<header className="navbar">
			<h1>Admin Dashboard</h1>
			{/* Add more navbar content (e.g., user profile, notifications) */}
		</header>
	);
};

export default Navbar;
